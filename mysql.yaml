# no need to write separate file we can parametraise the all componnets
- name: configure {{ component }} component
  become: yes
  hosts: "{{ component }}"
  # vars_files:
  # - vault/mysql.yaml
  roles: # this automatically load
  - "{{ component }}"
